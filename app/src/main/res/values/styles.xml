<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="AlertDialogCustom" parent="Theme.AppCompat.Light.Dialog.Alert">
        <!-- 다이얼로그 배경색 -->
        <item name="android:background">#f1efeb</item>
    </style>
"
    <style name="SpinnerDropdownStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:background">@color/white</item>
<!--        <item name="android:layout_height">300dp</item>  -->

    </style>

    <!-- 긍정 버튼 스타일 -->
    <style name="AlertDialogButton.Positive" parent="Widget.AppCompat.Button.ButtonBar.AlertDialog">
        <!-- 텍스트 색상을 여기서 설정 -->
        <item name="android:textColor">#00418c</item>
    </style>

    <!-- 부정 버튼 스타일 -->
    <style name="AlertDialogButton.Negative" parent="Widget.AppCompat.Button.ButtonBar.AlertDialog">
        <!-- 텍스트 색상을 여기서 설정 -->
        <item name="android:textColor">#00418c</item>
    </style>

</resources>